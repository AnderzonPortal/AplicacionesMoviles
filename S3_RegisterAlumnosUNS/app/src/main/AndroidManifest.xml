<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.S3_RegisterAlumnosUNS"
        tools:targetApi="31">

        <!-- Declaración de la actividad ListaAlumnosActivity -->
        <activity android:name=".ListaAlumnosActivity" />

        <!-- LoginActivity como actividad inicial -->
        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity ahora no es la actividad principal al iniciar -->
        <activity android:name=".MainActivity" />
    </application>

</manifest>
